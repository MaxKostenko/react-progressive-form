import styled from 'styled-components'

const FormCheckerElement = styled.label`
    display: block;
    text-align: left;
    margin: 15px 0 5px 0;
    cursor: pointer;
    user-select: none;
`;

export default FormCheckerElement